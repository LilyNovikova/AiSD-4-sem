#include "Node.h"

/*template <typename T>
inline Node<T>::~Node() {}

template<typename T>
void Node<T>::destroy_from_this(Node * node)
{
	while (node)
	{
		destroy_from_this(node->next);
		delete node;
	}
}

template<typename T>
T Node<T>::get_item() {return item;}*/

/*template<typename T>
Node<T> * Node<T>::get_next(){	return next;}

template<typename T>
 void Node<T>::set_next(Node<T>* next_node) { next = next_node; }*/
